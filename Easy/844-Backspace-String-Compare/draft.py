# LeetCode 844 — 比较含退格的字符串
#
# 题目描述：给定两个字符串 s 和 t，'#' 表示退格（删除前一个字符）。
# 把 s 和 t 分别输入到空白编辑器后，判断两者得到的最终字符串是否相等。
#
# 示例 1：输入: s = "ab#c", t = "ad#c" → 输出: True（两者都变成 "ac"）
# 示例 2：输入: s = "ab##", t = "c#d#" → 输出: True（两者都变成 ""）
# 示例 3：输入: s = "a#c", t = "b" → 输出: False（分别变成 "c" 和 "b"）

class Solution:
    def backspaceCompare(self, s: str, t: str) -> bool:
        pass
