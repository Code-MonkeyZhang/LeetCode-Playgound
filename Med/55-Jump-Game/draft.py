# LeetCode 55 — 跳跃游戏
#
# 题目描述：给你一个非负整数数组 nums，你最初位于数组的第一个下标。数组中的每个元素代表你在该位置可以跳跃的最大长度。
# 判断你是否能够到达最后一个下标，如果可以，返回 true；否则，返回 false。
#
# 示例 1：输入: nums = [2,3,1,1,4] → 输出: true（先跳1步到下标1，再跳3步到达最后一个下标）
# 示例 2：输入: nums = [3,2,1,0,4] → 输出: false（无论如何都会到达下标3，该处最大跳跃长度为0，无法继续前进）

from typing import List


class Solution:
    def canJump(self, nums: List[int]) -> bool:
        pass
